services:
  - type: web
    name: aegis-ai-trader
    env: node
    region: oregon
    plan: free
    branch: main
    buildCommand: ./render-build.sh
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
